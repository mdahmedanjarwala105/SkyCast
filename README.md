# SkyCast
SkyCast fetches live weather data and pairs it with natural‑language insights powered by an LLM. The app is split into a lightweight Python backend API and a static frontend (plain HTML/JS) for maximum simplicity.
